[profile.default]
src      = "contracts"
out      = "out"
libs     = ["lib"]
solc     = "0.8.20"
optimizer = true
optimizer_runs = 200

remappings = [
    "@aave/core-v3/=lib/aave-v3-core/",
    "@openzeppelin/=lib/openzeppelin-contracts/",
]

[profile.default.rpc_endpoints]
base = "${RPC_URL}"
